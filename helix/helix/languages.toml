[[language]]
name = "python"
formatter = { command = "ruff", args = ["format", "--line-length", "88", "-"] }
auto-format = true

[[language]]
name = "java"
indent = { tab-width = 4, unit = "    " }
formatter = { command = "mvn", args = ["fmt:format"] }
auto-format = true

[[language]]
name = "scala"
file-types = ["scala", "sbt"]
# formatter = {command = "sbt", args = [""]}
auto-format = false
